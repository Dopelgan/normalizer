# Рантайм сервиса. Единственный источник зависимостей — Dockerfile.dev
# ставит именно этот файл (poetry из проекта убран).

fastapi==0.115.6
uvicorn[standard]==0.34.0
celery==5.4.0
redis==5.2.1
psycopg2-binary==2.9.10
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.6.1
python-dotenv==1.0.1
# Профиль приёма: правила отсева вынесены из кода в YAML.
PyYAML==6.0.2
# Метрики: гистограммы длительности этапов на /metrics. Замер устроен так,
# что без этого пакета сервис работает — тайминги остаются в логах и в
# ответе, пропадают только метрики.
prometheus-client==0.21.1
boto3==1.35.69
botocore==1.35.69
requests==2.32.3
# Текстовый слой векторных PDF: источник истины по тексту и по
# геометрии чертежей. Без него конвейер работает, но падает на OCR.
PyMuPDF==1.24.14
# Уровень 1 лестницы: чтение исходников САПР (DXF).
ezdxf==1.3.5
# Уровень 2: извлечение текста и таблиц из DOCX.
python-docx==1.1.2
# Уровень 3: табличный разбор жёсткого формата.
openpyxl==3.1.5
# Excel 97-2003: openpyxl этот формат не открывает, а счета и УПД из
# бухгалтерских выгрузок приходят именно в нём.
xlrd==2.0.1
# Безопасный XML-парсер: openpyxl подхватывает его сам и перестаёт
# зависеть от того, чем именно lxml собран в конкретном образе.
defusedxml==0.7.1
# Оценка пригодности растра и его восстановление.
numpy==1.26.4
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.4.0
