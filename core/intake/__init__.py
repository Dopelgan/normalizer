"""
Приём документов, вынесенный из HTTP-запроса в очередь.

Data Gateway и Quality Gate остаются отдельными сервисами со своими
ручками, но сама работа — чтение файла, открытие формата, быстрый OCR —
идёт в задаче Celery. Ручка приёма ставит задачу и отвечает 202, вердикты
забираются поллингом.

Повод: пятьдесят PDF без текстового слоя не укладывались ни в таймаут
между Data Gateway и Quality Gate, ни в таймаут внешнего потребителя.
Потребитель, не дождавшись ответа, начинал слать запрос заново — и
конвейер получал ту же пачку по второму кругу, поверх ещё не законченной
первой. Таймауты подняли (120 -> 600 и 60 -> 300), но это отодвигает
границу, а не убирает её: следующий набор файлов будет сложнее.

Здесь границы нет вовсе. Ответ отдаётся сразу, файл обрабатывается
столько, сколько нужно, и повторный запрос по тому же request_id не
запускает работу заново.
"""

from core.intake.pipeline import IntakePipeline, evaluate_file  # noqa: F401
