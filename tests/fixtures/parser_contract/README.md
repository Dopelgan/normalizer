# Parser test fixtures

Generated for the Parser contract. See `manifest.json` for expected behavior.

Recommended intake batch: `valid_mixed.pdf`, `valid_mixed.docx`, `valid_table.xlsx`, `valid_drawing.dxf`, `valid_image.png`, `личное_отпуск.jpg`, `unsupported.dwg`, `corrupt.pdf`.
